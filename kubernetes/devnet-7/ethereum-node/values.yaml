# ANSIBLE GENERATED FILE - DO NOT EDIT MANUALLY
ethereum-node:
  global:
    main:
      network: 4844-devnet-7
      engineEndpoint: http://nethermind-lodestar-001:8551
      rpcEndpoint: http://nethermind-lodestar-001:8545
      beaconEndpoint: http://lodestar-nethermind-001:5052
    checkpointSync:
      enabled: false
  nethermind:
    fullnameOverride: nethermind-lodestar-001
    enabled: true
    image:
      repository: nethermindeth/nethermind
      tag: eip-4844-v6-36
      pullPolicy: Always
    p2pNodePort:
      enabled: false
    persistence:
      enabled: true
      size: 50Gi
    podLabels:
      network: 4844-devnet-7
      testnet: 4844-devnet-7
      ethereum_network: 4844-devnet-7
      ethereum_role: execution
      consensus_client: lodestar
      execution_client: nethermind
      ethereum_instance: nethermind-lodestar-001
    resources:
      requests:
        cpu: 2000m
        memory: 5Gi
      limits:
        cpu: 3000m
        memory: 7Gi
    extraArgs:
      - --Init.ChainSpecPath=/data/network-config/chainspec.json
      - --JsonRpc.EnabledModules=Eth,Subscribe,Trace,TxPool,Web3,Personal,Proof,Net,Parity,Health,Rpc,Debug,Admin
      - --Discovery.Bootnodes="$(tr '\n' ',' < /data/ethereum-network-config/bootnode.txt)"
      - --Init.IsMining=false
      - --Pruning.Mode=None
      - --config=none.cfg
      - --EthStats.Enabled=true
      - --EthStats.Name=k8s-nethermind-lodestar
      - --EthStats.Secret=eip4844SuperSecret
      - --EthStats.Server=wss://ethstats.4844-devnet-7.ethpandaops.io/api/
      - --log=DEBUG
    initContainers:
    - name: init-genesis
      image: alpine:latest
      imagePullPolicy: IfNotPresent
      securityContext:
        runAsNonRoot: false
        runAsUser: 0
      command:
        - sh
        - -ace
        - >
          GENESIS_URI=https://config.4844-devnet-7.ethpandaops.io/el/chainspec.json;
          TRUSTED_SETUP_URI=https://config.4844-devnet-7.ethpandaops.io/trusted_setup.txt;
          BOOTNODES=https://config.4844-devnet-7.ethpandaops.io/el/bootnode.txt;
          mkdir -p /data/network-config;
          wget -O /data/network-config/bootnode.txt $BOOTNODES;
          if ! [ -f /data/network-config/genesis_init_done ];
          then
            apk update && apk add curl jq;
            wget -O /data/network-config/genesis.json $GENESIS_URI;
            wget -O /data/network-config/trusted_setup.txt $TRUSTED_SETUP_URI;
            cat /data/network-config/genesis.json | jq -r '.config.chainId' > /data/network-config/chainid.txt;
            touch /data/network-config/genesis_init_done;
            echo "genesis init done";
          else
            echo "genesis is already initialized";
          fi;
          echo "bootnode init done: $(cat /data/network-config/bootnode.txt)";
      volumeMounts:
        - name: storage
          mountPath: "/data"
  lodestar:
    fullnameOverride: lodestar-nethermind-001
    enabled: true
    image:
      repository: g11tech/lodestar
      tag: devnet36-6c23a8
      pullPolicy: Always
    mode: "beacon"
    podLabels:
      network: 4844-devnet-7
      testnet: 4844-devnet-7
      ethereum_network: 4844-devnet-7
      ethereum_role: consensus
      consensus_client: lodestar
      execution_client: nethermind
      ethereum_instance: nethermind-lodestar-001
    resources:
      requests:
        cpu: 2000m
        memory: 5Gi
      limits:
        cpu: 3000m
        memory: 7Gi
    p2pNodePort:
      enabled: false
      startsAt: 31004
    persistence:
      enabled: true
      size: 50Gi
    extraArgs:
      - --paramsFile=/data/network-config/config.yaml
      - --genesisStateFile=/data/network-config/genesis.ssz
      - --rest.namespace="*"
      - --bootnodes=/data/network-config/boot_enr.txt
      - --network.connectToDiscv5Bootnodes
      - --nat=true
    initContainers:
    - name: init-genesis
      image: alpine:latest
      imagePullPolicy: IfNotPresent
      securityContext:
        runAsNonRoot: false
        runAsUser: 0
      command:
        - sh
        - -ace
        - >
          apk add jq curl sed;
          DEPOSIT_CONTRACT_URI=https://config.4844-devnet-7.ethpandaops.io/deposit_contract.txt;
          DEPOSIT_CONTRACT_BLOCK_URI=https://config.4844-devnet-7.ethpandaops.io/cl/deposit_contract_block.txt;
          DEPLOY_BLOCK_URI=https://config.4844-devnet-7.ethpandaops.io/cl/deploy_block.txt;
          GENESIS_CONFIG_URI=https://config.4844-devnet-7.ethpandaops.io/cl/config.yaml;
          GENESIS_SSZ_URI=https://config.4844-devnet-7.ethpandaops.io/cl/genesis.ssz;
          BOOTNODES=https://config.4844-devnet-7.ethpandaops.io/cl/boot_enr.yaml;
          TRUSTED_SETUP_URI=https://config.4844-devnet-7.ethpandaops.io/trusted_setup.txt;
          mkdir -p /data/network-config;
          wget -O /data/network-config/boot_enr.yaml $BOOTNODES;
          if ! [ -f /data/network-config/genesis.ssz ];
          then
            wget -O /data/network-config/deposit_contract.txt $DEPOSIT_CONTRACT_URI;
            wget -O /data/network-config/deposit_contract_block.txt $DEPOSIT_CONTRACT_BLOCK_URI;
            wget -O /data/network-config/deploy_block.txt $DEPLOY_BLOCK_URI;
            wget -O /data/network-config/config.yaml $GENESIS_CONFIG_URI;
            wget -O /data/network-config/genesis.ssz $GENESIS_SSZ_URI;
            wget -O /data/network-config/trusted_setup.txt $TRUSTED_SETUP_URI;
            echo "genesis init done";
          else
            echo "genesis exists. skipping...";
          fi;
          echo "bootnode init done: $(cat /data/network-config/boot_enr.txt)";
      volumeMounts:
        - name: storage
          mountPath: "/data"
# ANSIBLE GENERATED FILE - DO NOT EDIT MANUALLY